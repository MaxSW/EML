Maths
	round(num, ?dp)
	floor(num)
	ceil(num)
	abs(num)
	sin(num)
	cos(num)
	tan(num)
	
	
Alert
	alert(message, ?title)
	info(message, ?title)
	warn(message, ?title)
	error(message, ?title)

Input
	getNumber(?message, ?title)

Random
	between(from, to)